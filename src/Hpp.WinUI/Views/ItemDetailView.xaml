<Page
    x:Class="Hpp.WinUI.Views.ItemDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Loaded="OnLoaded"
    Background="Transparent">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid Padding="20" RowDefinitions="Auto,Auto,*" RowSpacing="12" MinWidth="1080">
            <Border Style="{ThemeResource GlassCardStyle}" Background="#2A0F243E">
                <Grid RowDefinitions="Auto,Auto" RowSpacing="10">
                    <StackPanel>
                        <TextBlock Text="Item Intelligence" Style="{ThemeResource HeroTextStyle}" FontSize="28" TextWrapping="Wrap" />
                        <TextBlock Text="Trace every lot, cost layer, and purchase stream for a single SKU." Style="{ThemeResource CaptionTextStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
                        <TextBox Width="220" PlaceholderText="SKU" Text="{Binding LookupSku, Mode=TwoWay}" />
                        <Button Content="Search" Command="{Binding SearchCommand}" Style="{ThemeResource PrimaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" ColumnDefinitions="*,*,*" ColumnSpacing="10">
                <Border Style="{ThemeResource GlassCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Item" Style="{ThemeResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding ItemName}" FontSize="24" FontWeight="SemiBold" Foreground="White" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding ItemCategory}" Foreground="#DCE8F8" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{ThemeResource GlassCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Standard Cost" Style="{ThemeResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding StandardCost}" FontSize="24" FontWeight="SemiBold" Foreground="White" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{ThemeResource GlassCardStyle}">
                    <StackPanel>
                        <TextBlock Text="On-Hand Value" Style="{ThemeResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding TotalOnHand}" FontSize="24" FontWeight="SemiBold" Foreground="White" />
                        <TextBlock Text="{Binding EstimatedValue}" Foreground="#DCE8F8" />
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2" ColumnDefinitions="2*,2*" ColumnSpacing="10">
                <Border Style="{ThemeResource GlassCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Inventory Lots" Style="{ThemeResource SectionHeaderTextStyle}" />
                        <ListView ItemsSource="{Binding Lots}" MinHeight="470">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="10" CornerRadius="10" Background="#2A243B5E">
                                        <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
                                            <TextBlock Text="{Binding ReceivedDate}" Foreground="#7DD3FC" />
                                            <TextBlock Grid.Column="1" Text="{Binding Quantity}" Foreground="White" />
                                            <TextBlock Grid.Column="2" Text="{Binding UnitCost}" Foreground="White" />
                                            <TextBlock Grid.Column="3" Text="{Binding TotalCost}" Foreground="#DCE8F8" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{ThemeResource GlassCardStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Recent Purchases" Style="{ThemeResource SectionHeaderTextStyle}" />
                        <ListView ItemsSource="{Binding RecentPurchases}" MinHeight="470">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="10" CornerRadius="10" Background="#2A243B5E">
                                        <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
                                            <TextBlock Text="{Binding PurchasedAt}" Foreground="#7DD3FC" />
                                            <TextBlock Grid.Column="1" Text="{Binding Quantity}" Foreground="White" />
                                            <TextBlock Grid.Column="2" Text="{Binding UnitCost}" Foreground="White" />
                                            <TextBlock Grid.Column="3" Text="{Binding Supplier}" Foreground="#DCE8F8" TextTrimming="CharacterEllipsis" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBlock Text="{Binding Status}" Style="{ThemeResource CaptionTextStyle}" />
                        <ProgressBar IsIndeterminate="{Binding IsBusy}" Height="6" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
