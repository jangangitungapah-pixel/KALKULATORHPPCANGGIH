<Page
    x:Class="Hpp.WinUI.Views.ItemDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Loaded="OnLoaded">

    <Grid Padding="24" RowDefinitions="Auto,Auto,*" ColumnDefinitions="2*,3*">
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Item Detail" FontSize="24" FontWeight="SemiBold" />
            <TextBox Width="240" PlaceholderText="SKU..." Text="{Binding LookupSku, Mode=TwoWay}" />
            <Button Content="Search" Command="{Binding SearchCommand}" />
            <ProgressBar IsIndeterminate="{Binding IsBusy}" Width="120" Height="8" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Status}" Foreground="Gray" VerticalAlignment="Center" />
        </StackPanel>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,16,0,0" ColumnDefinitions="*,*,*">
            <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Item" Foreground="Gray" />
                    <TextBlock Text="{Binding ItemName}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding ItemCategory}" Foreground="Gray" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="8,0,8,0">
                <StackPanel>
                    <TextBlock Text="Standard Cost" Foreground="Gray" />
                    <TextBlock Text="{Binding StandardCost}" FontWeight="SemiBold" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="On Hand / Value" Foreground="Gray" />
                    <TextBlock Text="{Binding TotalOnHand}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding EstimatedValue}" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,16,0,0" ColumnDefinitions="2*,2*">
            <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Lots" FontWeight="SemiBold" />
                    <ListView ItemsSource="{Binding Lots}" Height="420">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,*,*,*" Padding="4">
                                    <TextBlock Text="{Binding ReceivedDate}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Quantity}" />
                                    <TextBlock Grid.Column="2" Text="{Binding UnitCost}" />
                                    <TextBlock Grid.Column="3" Text="{Binding TotalCost}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Recent Purchases" FontWeight="SemiBold" />
                    <ListView ItemsSource="{Binding RecentPurchases}" Height="420">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,*,*,*" Padding="4">
                                    <TextBlock Text="{Binding PurchasedAt}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Quantity}" />
                                    <TextBlock Grid.Column="2" Text="{Binding UnitCost}" />
                                    <TextBlock Grid.Column="3" Text="{Binding Supplier}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
