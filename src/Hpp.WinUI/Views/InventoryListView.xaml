<Page
    x:Class="Hpp.WinUI.Views.InventoryListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,Auto,*" ColumnDefinitions="2*,3*"
          Loaded="OnLoaded">
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Inventory" FontSize="24" FontWeight="SemiBold" />
            <Button Content="Refresh" Command="{Binding LoadCommand}" />
            <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" AutomationProperties.Name="Export inventory to CSV" />
            <Button Content="Export Excel" Command="{Binding ExportExcelCommand}" AutomationProperties.Name="Export inventory to Excel" />
            <TextBlock Text="{Binding LastUpdated}" Foreground="Gray" VerticalAlignment="Center" />
        </StackPanel>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" ColumnDefinitions="2*,2*,2*,2*,Auto,Auto" Margin="0,16,0,0">
            <TextBox Header="Search" Text="{Binding FilterText, Mode=TwoWay}" TextChanged="OnFilterChanged" Margin="0,0,12,0" />
            <ComboBox Grid.Column="1" Header="Category" ItemsSource="{Binding Categories}" SelectedItem="{Binding FilterCategory, Mode=TwoWay}" SelectionChanged="OnFilterChanged" Margin="0,0,12,0" />
            <NumberBox Grid.Column="2" Header="Min Cost" Value="{Binding MinCost, Mode=TwoWay}" ValueChanged="OnNumberFilterChanged" Margin="0,0,12,0" />
            <NumberBox Grid.Column="3" Header="Max Cost" Value="{Binding MaxCost, Mode=TwoWay}" ValueChanged="OnNumberFilterChanged" Margin="0,0,12,0" />
            <ComboBox Grid.Column="4" Header="Sort By" ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SortBy, Mode=TwoWay}" SelectionChanged="OnFilterChanged" Margin="0,0,12,0" />
            <ToggleSwitch Grid.Column="5" Header="Desc" IsOn="{Binding SortDescending, Mode=TwoWay}" Toggled="OnFilterToggled" />
        </Grid>

        <Grid Grid.Row="2" ColumnDefinitions="2*,3*" Margin="0,16,0,0">
            <StackPanel Grid.Column="0" Spacing="12" Margin="0,0,16,0">
                <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid ColumnDefinitions="*,*,*">
                        <StackPanel>
                            <TextBlock Text="Total Items" Foreground="Gray" />
                            <TextBlock Text="{Binding TotalItems}" FontSize="18" FontWeight="SemiBold" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Total Value" Foreground="Gray" />
                            <TextBlock Text="{Binding TotalInventoryValue}" FontSize="18" FontWeight="SemiBold" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Avg Cost" Foreground="Gray" />
                            <TextBlock Text="{Binding AverageCost}" FontSize="18" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" />
                        <Button Content="Duplicate" Command="{Binding DuplicateSelectedCommand}" />
                    </StackPanel>
                </Border>

                <TextBlock Text="Items" FontWeight="SemiBold" />
                <ListView ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" Height="480">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="Auto,*,*,Auto" Padding="4">
                                <TextBlock Text="{Binding Sku}" Width="120" />
                                <TextBlock Grid.Column="1" Text="{Binding Name}" Width="200" />
                                <TextBlock Grid.Column="2" Text="{Binding Category}" Width="140" />
                                <TextBlock Grid.Column="3" Text="{Binding StandardCost}" Width="100" HorizontalAlignment="Right" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <ScrollViewer Grid.Column="1">
                <StackPanel Spacing="16">
                    <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Create item" FontWeight="SemiBold" />
                            <TextBox Header="SKU" Text="{Binding NewSku, Mode=TwoWay}" />
                            <TextBox Header="Name" Text="{Binding NewName, Mode=TwoWay}" />
                            <TextBox Header="Category" Text="{Binding NewCategory, Mode=TwoWay}" />
                            <NumberBox Header="Standard Cost" Value="{Binding NewStandardCost, Mode=TwoWay}" />
                            <Button Content="Create" Command="{Binding CreateItemCommand}" />
                        </StackPanel>
                    </Border>

                    <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Selected item" FontWeight="SemiBold" />
                            <TextBox Header="SKU" Text="{Binding EditSku, Mode=TwoWay}" />
                            <TextBox Header="Name" Text="{Binding EditName, Mode=TwoWay}" />
                            <TextBox Header="Category" Text="{Binding EditCategory, Mode=TwoWay}" />
                            <NumberBox Header="Standard Cost" Value="{Binding EditStandardCost, Mode=TwoWay}" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="Update" Command="{Binding UpdateSelectedCommand}" />
                                <Button Content="Delete" Command="{Binding DeleteSelectedCommand}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="6">
                            <TextBlock Text="Status" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" TextWrapping="Wrap" />
                            <ProgressBar IsIndeterminate="{Binding IsBusy}" Height="8" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
