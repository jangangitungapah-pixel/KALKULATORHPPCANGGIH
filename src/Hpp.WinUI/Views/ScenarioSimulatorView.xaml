<Page
    x:Class="Hpp.WinUI.Views.ScenarioSimulatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <StackPanel Padding="24" Spacing="12">
        <TextBlock Text="Scenario Simulator" FontSize="24" FontWeight="SemiBold" />
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBox Header="Scenario name" Text="{Binding ScenarioName, Mode=TwoWay}" Width="220" />
            <TextBox Header="Adjustment %" Text="{Binding AdjustmentPercent, Mode=TwoWay}" Width="160" />
            <Button Content="Run" Command="{Binding RunSimulationCommand}" AutomationProperties.Name="Run scenario simulation" />
        </StackPanel>
        <TextBlock Text="{Binding ResultText}" TextWrapping="Wrap" />
    </StackPanel>
</Page>
