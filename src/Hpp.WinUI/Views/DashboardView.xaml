<Page
    x:Class="Hpp.WinUI.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:local="using:Hpp.WinUI.Views"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,*" ColumnDefinitions="*,*">
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="SemiBold" />
            <Button Content="Seed Sample Data" Command="{Binding SeedCommand}" AutomationProperties.Name="Seed sample data" />
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="0" Margin="0,16,12,0" Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Spacing="8">
                <TextBlock Text="HPP Trend (Placeholder)" FontWeight="SemiBold" />
                <ProgressBar Value="65" Height="24" />
                <TextBlock Text="TODO: Replace with interactive chart." Foreground="Gray" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Margin="12,16,0,0" Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Spacing="12">
                <TextBlock Text="Top SKUs" FontWeight="SemiBold" />
                <controls:DataGrid ItemsSource="{Binding TopSkus}" AutoGenerateColumns="False" IsReadOnly="True" Height="300">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="*" />
                        <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                        <controls:DataGridTextColumn Header="Cost" Binding="{Binding StandardCost}" Width="*" />
                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
