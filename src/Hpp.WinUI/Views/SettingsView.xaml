<Page
    x:Class="Hpp.WinUI.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid Padding="20" RowDefinitions="Auto,*" RowSpacing="12" MinWidth="860">
            <Border Style="{ThemeResource GlassCardStyle}" Background="{ThemeResource BrandHeroSurfaceBrush}">
                <StackPanel>
                    <TextBlock Text="Experience Controls" Style="{ThemeResource HeroTextStyle}" FontSize="28" TextWrapping="Wrap" />
                    <TextBlock Text="Tune display modes and operational safeguards for the finance cockpit." Style="{ThemeResource CaptionTextStyle}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Grid Row="1" ColumnDefinitions="2*,3*" ColumnSpacing="12">
                <Border Style="{ThemeResource GlassCardStyle}">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Theme &amp; Interface" Style="{ThemeResource SectionHeaderTextStyle}" />
                        <ToggleSwitch Header="Dark Theme" IsOn="{Binding IsDarkTheme, Mode=TwoWay}" />
                        <ToggleSwitch Header="Confirm Delete Actions" IsOn="{Binding ConfirmDeleteMode, Mode=TwoWay}" />
                        <ComboBox Header="Accent Palette" ItemsSource="{Binding AccentOptions}" SelectedItem="{Binding SelectedAccent, Mode=TwoWay}" />
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                            <Button Content="Apply" Command="{Binding ApplyCommand}" Style="{ThemeResource PrimaryButtonStyle}" />
                            <Button Grid.Column="1" Content="Reset" Command="{Binding ResetCommand}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{ThemeResource GlassCardStyle}">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Status" Style="{ThemeResource SectionHeaderTextStyle}" />
                        <TextBox Text="{Binding StatusMessage}" IsReadOnly="True" AcceptsReturn="True" Height="220" />
                        <TextBlock Text="Profile Notes" Style="{ThemeResource SectionHeaderTextStyle}" />
                        <TextBlock Text="Theme and accent changes apply instantly across shell surfaces, cards, controls, and title bar colors." Style="{ThemeResource CaptionTextStyle}" TextWrapping="Wrap" />
                        <Border Style="{ThemeResource PillBadgeStyle}" HorizontalAlignment="Left">
                            <TextBlock Text="UI profile: Next Gen" Foreground="{ThemeResource AppTextPrimaryBrush}" />
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>

