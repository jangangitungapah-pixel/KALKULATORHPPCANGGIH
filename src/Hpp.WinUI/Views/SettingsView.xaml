<Page
    x:Class="Hpp.WinUI.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <StackPanel Padding="24" Spacing="12">
        <TextBlock Text="Settings" FontSize="24" FontWeight="SemiBold" />
        <ToggleSwitch Header="Dark theme" IsOn="{Binding IsDarkTheme, Mode=TwoWay}" AutomationProperties.Name="Toggle dark theme" />
        <ToggleSwitch Header="Confirm delete for destructive actions" IsOn="{Binding ConfirmDeleteMode, Mode=TwoWay}" />
        <TextBlock Text="Accent color" />
        <ComboBox ItemsSource="{Binding AccentOptions}" SelectedItem="{Binding SelectedAccent, Mode=TwoWay}" Width="240" />
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="Apply" Command="{Binding ApplyCommand}" />
            <Button Content="Reset" Command="{Binding ResetCommand}" />
        </StackPanel>
        <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" TextWrapping="Wrap" />
    </StackPanel>
</Page>
